# Secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# App data
projects/
*.db
*.db-journal
*.db-wal
*.db-shm
logs/
*.log

# Models cache
models/
.cache/

# Runtime: PO-token provider exe (46MB) + COOKIE đăng nhập (NHẠY CẢM!) + cache
_potoken/
_cache/
_fp.png
_download/
KhoVideo/
.env.local

# OS
.DS_Store
Thumbs.db

# Build
build/
dist/
*.spec
.anon_key.tmp
app.lock

_build*/
_dist*/
_venv_client/
bin_audit/
_hooks_test/

# build local
bin/
.venv-build/
_perf/
_tts_voices.json
_tmp_*.py
_do_chon_cache/

# Rác của lượt kiểm (video/log/ảnh — TUYỆT ĐỐI không commit, 135 MB/lượt)
_clip_that/
_ketqua_log/
_kiem_ketqua*.json
_ket_*.json
_ket_*.txt
_mut_*.txt

# Thay giọng: thư mục đo + thư viện Demucs (nặng, KHÔNG commit)
_tg/
_lib/
_tq_work/

# Piper TTS: CHƯƠNG TRÌNH RỜI + giọng vais1000 (~150 MB), app TỰ TẢI khi user
# chọn dùng. KHÔNG commit, KHÔNG đóng vào .exe — đó chính là ranh giới giấy
# phép GPL đã chốt ở docs/GIONG_DOC_MIEN_PHI.md.
_piper/
_do_piper/
_giong_hang/

# Giọng ngoài (OmniVoice): môi trường Python RIÊNG ~7,7 GB + runner sinh lúc
# chạy. Cùng luật `_piper`/`_lib`: KHÔNG commit, KHÔNG đóng vào .exe.
# Thư mục này CỐ Ý nằm cạnh repo (chạy nguồn) / trong DATA_DIR (bản .exe) chứ
# KHÔNG nằm trong `%TEMP%` — xem `giong_ngoai.thu_muc_ngoai`.
_giong_ngoai/
# VieNeu: CUNG LUAT — moi truong Python RIENG (tai roi luc chay, khong phai ma
# nguon). Truoc 19/08/2026 no KHONG duoc chan, tuc mot luot `git add -A` la ca
# thu muc vao lich su git.
_giong_vieneu/
_do_gn_san/
_do_gn_gh_san/

# Worktree tạm của phiên làm việc — một BẢN SAO CẢ REPO, lỡ `git add -A` là
# commit vài trăm MB. KHÔNG xoá tay ở đây, chỉ chặn không cho vào commit.
.claude/
# Thư mục làm việc của `_do_tempo_cau.py` / `_do_kho_tg.py` (mp3/wav từng câu)
_do_tempo_tam/
_do_tg_kho/

# Thu muc nhap cua luot do pip (cong 58) — khong bao gio commit
_tmp_dry/

# Nhat ky luot chay hoi quy (_hq_<ten cong>.txt) — rac cua phep do
_hq_*.txt
_do_lt/

# San do cua luot chep loi / khop tieng / rubberband (506 + 75 + 28 MB wav+json)
# — deu la SAN DO tai sinh lai duoc, TUYET DOI khong commit.
_do_chep_san/
_do_khop_san/
_do_rb_tam/
_do_rb_cache.json

# San do cua `_do_khop_video.py` (do GHEP CAP arm CU vs arm MOI): wav/mp4 cua
# ca hai arm + manh khop tung cau + lop nhac da gian. Tai sinh lai duoc.
_do_kv_tam/

# mp3/wav cua `_do_bien_the_giong.py` — de NGHE bang tai, khong commit
_do_bt_giong/

# LUOT GIONG 19/08/2026 — deu la SAN DO / THU VIEN TAI ROI, tai sinh lai duoc:
#   _lib_giong/       edge-tts cai RIENG de do (KHONG cai vao `.venv`: luong
#                     khac dang dung lai `.venv`, chen vao la pha viec ho)
#   _giong_chatter/   moi truong Python rieng cua Chatterbox (torch CUDA ~7 GB)
#   bq_do_nhan_nha*/  mp3/wav cua 191 luot do nhan nha
#   _do_chatter/      san do cua Chatterbox (wav + ket qua)
#   _mau_giong/       MAU GIONG dua vao de nhan ban — TUYET DOI khong commit:
#                     do la giong NGUOI THAT, va file mau khong phai cua repo.
#   _kq*.json         cung ly do bao mat voi `_kq*.txt` o duoi.
_lib_giong/
_giong_chatter/
bq_do_nhan_nha*/
_do_chatter/
_mau_giong/
_kq*.json

# SAN DO cua `_do_song_ngu.py` (do "giong nao doc duoc CA Anh + Viet"):
# 40+ arm x 17 file mp3 + ban chep nguoc. Tai sinh lai duoc tu cache.json.
_do_song_ngu/

# SAN DO cua `_do_adam.py` (Adam VieNeu vs Adam ElevenLabs) + cho de file
# tieng anh Hung TU NGHE. Deu la wav/mp3 sinh lai duoc, va mot phan trong do
# la tieng cua MAY DOC THUONG MAI (ElevenLabs) — khong dua vao lich su git.
_do_adam/
_NGHE_THU_ANH_HUNG/

# BAO MAT: cong 70 tung in NGUYEN VAN key Groq that ra file ket qua.
# Mot luot `git add -A` la key vao lich su git cong khai. Chan tan goc.
_kq*.txt
_kq_hq/
_kq*/
bq_test_dg86*/

# Hộp cát phép đo giọng nhân bản (WAV mẫu + tiếng sinh ra, hàng trăm MB)
_do_giong_toi/

# Hop cat phep do "doc lai cau lan man" (_do_vnb_lan.py / _do_vnb_doclai.py):
# WAV mau + tieng sinh ra + ban sao luot doc dau dung chung, hang tram MB.
# Ban GON co theo doi git la _moc_doc_lan.json (cong 92 doc file do).
_do_vnb_doclai/
_do_vnb_en/

# LUOT "KHO KHAN, KHONG CAM XUC" 27/08/2026 — HOP CAT cua 4 phep do, deu tai
# sinh lai duoc (chay lai chinh script do):
#   bq_do_mau_dai/    30 MB — file MAU giong + 10 luot nhan ban cua phep do
#                     ghep cap 2x2 "mau dai / mau co nhan nha". Mau la giong
#                     MAY (edge-tts + VieNeu dung san), KHONG phai nguoi that.
#   bq_do_san_vnb/    21 MB — 8 luot doc 20 cau de do SAN trai toc do doc.
#   bq_do_el_camxuc/  hop cat cua phep chung minh eleven_v3 (tu don).
#   bq_nghe_thu_cx/   hop cat sinh file nghe thu (tu don).
bq_do_mau_dai/
bq_do_san_vnb/
bq_do_el_camxuc/
bq_nghe_thu_cx/
_do_dc_tam/
_do_ct_tam/
_do_gd_tam/
_do_rate_am_tam/
_do_gd_chep.json
# VIẾT ĐẦY CÂU HỤT KHUNG (bước 4b') — hộp cát + cache của `_do_viet_day.py`
# và `_do_bon_chu.py`. Trong đó có bộ file giọng THẬT (hàng trăm MB) và cache
# lượt chấm LLM; không có gì để git theo dõi.
_do_vd_tam/
_do_bc_tam/
_do_bc_cache/
