======================================================================
CỔNG 58 — `_lib` tự đứng được + phép dò nói thật
======================================================================

_lib THẬT của máy này: D:\claude\ai-content-studio\_lib
  tồn tại: True

=== CA 1: GIẢ LẬP MÔI TRƯỜNG .exe (không có `.venv` để mượn) ===
  máy DEV (có .venv) : thiếu=['torch', 'soundfile'] · co=True · du_lib=False
  giả lập .exe       : thiếu=['torch', 'soundfile'] · co=False · du_lib=False
  nguồn từng gói (dev): {'torch': 'hệ thống', 'demucs': '_lib', 'soundfile': 'hệ thống'}
  ĐẠT  CA1a máy DEV và bản .exe nói GIỐNG NHAU về `_lib` (mệnh đề trung tâm)  [dev=['torch', 'soundfile'] · exe=['torch', 'soundfile']]
  ĐẠT  CA1b `du_lib` cũng giống nhau ở hai môi trường  [dev=False · exe=False]
  ĐẠT  CA1c trong môi trường .exe, `co_demucs()` ĐÚNG BẰNG '`_lib` có đủ'  [co=False · thiếu=['torch', 'soundfile']]
  ĐẠT  CA1d `_lib` ĐỦ 3 gói -> môi trường .exe báo CÓ (không phải luôn False)  [co=True · thiếu=[]]
  ĐẠT  CA1e và nguồn của cả 3 gói phải ghi là `_lib`  [{'torch': '_lib', 'demucs': '_lib', 'soundfile': '_lib'}]

=== CA 2: TRẠNG THÁI CÀI DỞ (có demucs, thiếu torch) ===
  ĐẠT  CA2a demucs dò ra nằm trong `_lib`  [D:\claude\ai-content-studio\bq_test_lib_85056\lib_do\demucs\__init__.py]
  ĐẠT  CA2b torch KHÔNG được coi là đã cài (dù `.venv` máy này có)  [nguồn=hệ thống · D:\claude\ai-content-studio\.venv\Lib\site-packages\torch\__]
  ĐẠT  CA2c `thieu` PHẢI có torch (không được báo đã cài)  [thiếu=['torch', 'soundfile']]
  ĐẠT  CA2d `du_lib` = False
  ĐẠT  CA2e gói đang MƯỢN của hệ thống phải được nêu đích danh  [ngoai_lib=['torch', 'soundfile']]
  ĐẠT  CA2f nút ghi 'Cài tiếp phần còn thiếu', KHÔNG phải 'Tải bộ tách giọng'  [Cài tiếp phần còn thiếu (torch, soundfile)]
  ĐẠT  CA2g nhãn nút nêu ĐÍCH DANH gói còn thiếu  [Cài tiếp phần còn thiếu (torch, soundfile)]
  ĐẠT  CA2h `_lib` RỖNG thì nút ghi 'tải cả bộ' (nhãn hai trạng thái khác nhau)  [Tải bộ tách giọng (tải khoảng 155 MB)]

=== CA 3: CÀI ĐỦ -> `spec.origin` CỦA CẢ 3 GÓI NẰM DƯỚI `_lib` ===
  ĐẠT  CA3a cả 3 gói dò ra nguồn `_lib`  [{'torch': '_lib', 'demucs': '_lib', 'soundfile': '_lib'}]
  ĐẠT  CA3b `spec.origin` của cả 3 THẬT SỰ nằm dưới `_lib`  [__init__.py · __init__.py · soundfile.py]
  ĐẠT  CA3c pip `--target --ignore-installed` ĐẶT ĐƯỢC gói mà `.venv` đã có vào đúng `_lib` (gói nhỏ, suy ra cho torch)  [rc=0 · D:\claude\ai-content-studio\bq_test_lib_85056\lib_pip\soundfile.py]
  ĐẠT  CA3d lệnh pip của `cai_demucs` có `--ignore-installed` (đọc bằng AST)
  ĐẠT  CA3e và vẫn cài vào `--target` (không đụng `.venv` đang chạy 300 kênh)

=== CA 4: THỬ PHÁ — trả `co_demucs` về kiểu `find_spec` cũ ===
  BẢN CŨ · máy DEV : thiếu=[] · co=True
  BẢN CŨ · .exe    : thiếu=['torch', 'soundfile'] · co=False
  ĐẠT  CA4a bản CŨ nói KHÁC NHAU ở hai môi trường -> CA1a sẽ FAIL (cổng không phải con dấu)  [dev=[] · exe=['torch', 'soundfile']]
  ĐẠT  CA4b bản CŨ trên máy dev báo 'đã cài' trong khi `_lib` thật sự thiếu  [cũ.co=True · `_lib` thiếu thật=['torch', 'soundfile']]
  ĐẠT  CA4c bản MỚI thì không lệch (đối chứng của CA4a)

======================================================================
CỔNG 58: ĐẠT 21 · HỎNG 0
======================================================================
