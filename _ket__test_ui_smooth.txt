== 1. DIALOG Qt mở: 'máy' dây chuyền PHẢI chạy tiếp ==
  ✓ dựng cảnh: có modal đang mở
  ✓ TỰ XUẤT clip chạy đủ 2/2 nhịp (hết 'phải đóng dialog')
  ✓ dõi dây chuyền chạy đủ 2/2 nhịp
  ✓ phần VẼ trang chính (bị dialog che) được bỏ -> đỡ tốn CPU
== 2. hộp CHỌN FILE native: phải dừng HẲN (giữ bản sửa cũ) ==
  ✓ đang mở hộp chọn thư mục -> KHÔNG nhịp nào chạy (không cướp focus)
== 3. 1 clip đổi trạng thái: KHÔNG đập cả danh sách ==
  ✓ có thanh tiến trình (video đang có job chạy)
  ✓ sổ dòng clip ghi đủ 3 dòng
  ✓ THANH TIẾN TRÌNH giữ nguyên widget (hết mất-rồi-lại-có)
  ✓ 2 dòng KHÔNG đổi được giữ nguyên (chỉ dựng lại 1 dòng)
  ✓ dòng vừa đổi ĐƯỢC cập nhật
     (cập nhật tại chỗ: 1.7 ms)
  ✓ cập nhật tại chỗ nhanh (< 8ms, đo 1.7ms — trước 9,1ms cho cả danh sách)
== 4. TẬP clip thay đổi -> vẫn dựng lại đầy đủ (không sai) ==
  ✓ thêm clip -> danh sách có 4 dòng
== 5. ngân sách 1 nhịp poll với 100 kênh ==
     (_poll_tick: 0.51 ms/nhịp)
  ✓ 1 nhịp poll < 30ms (đo 0.51ms) — UI không thể là nguồn đơ

KẾT QUẢ: TẤT CẢ ĐẠT — dialog mở vẫn chạy, danh sách không nhấp nháy
