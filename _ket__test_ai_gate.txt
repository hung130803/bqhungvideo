  ✓ dựng cảnh: video đã vào dây chuyền

══ 1. _video_cut_basic ══
  ✓ chưa có clip -> KHÔNG coi là cơ bản
  ✓ clip llm_used=False -> CƠ BẢN
  ✓ có clip qua AI -> KHÔNG phải cơ bản

══ 2. cơ bản lần 1 -> tự phân tích lại ══
  ✓ đã xếp job phân tích LẠI (1 -> 2 job)
  ✓ CHƯA xuất clip cơ bản nào
  ✓ log báo rõ 'tự PHÂN TÍCH LẠI'
  ✓ sổ chờ-xuất đã trỏ sang job mới

══ 3. cơ bản lần 2 -> xuất + đánh dấu ══
  ✓ KHÔNG phân tích lại lần 3 (tránh vòng lặp)
  ✓ job đã ra khỏi sổ chờ-xuất (đã xử lý)
  ✓ ctx được đánh dấu basic=True (khi xuất chạy được)
  ✓ log ⚠ báo 'vẫn cắt CƠ BẢN sau 2 lần'

══ 4. video qua AI xuất thẳng ══
  ✓ KHÔNG phân tích lại video đã qua AI
  ✓ không bị đánh dấu [CƠ BẢN] oan

KẾT QUẢ: TẤT CẢ ĐẠT — nhìn được video nào qua AI, video nào không
